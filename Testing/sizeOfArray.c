/* #include <stdio.h>
void checkSizeOfFloat(float arr[], int size);

int main(){
	float arr = {20.1, 40.2, 60.3, 80.4, 100.5, 120.6};
	int e = arr[0]
	int size = sizeof(arr) / sizeof(arr[0]);
	printf("The size of the float array is %i");

void checkSizeOfFloat(float arr[], int size) {
	
}
 */